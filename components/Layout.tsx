
import React, { ReactNode } from 'react';

// This file is no longer used and its contents can be removed.
// The new layout logic is handled by MainAppLayout.tsx for better structure and responsiveness.
const Layout: React.FC<{ children: ReactNode }> = ({ children }) => {
  return <>{children}</>;
};

export default Layout;
